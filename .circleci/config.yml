version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:2021.11
        auth:
          username: ahteshamdocker
          password: realme@1234  # context / project UI env-var reference
    resource_class: ahteshamahmad/rhl_machine
    steps:
      - checkout
      - run: echo "Hi I'm on Runners!"

workflows:
  build-workflow:
    jobs:
      - build