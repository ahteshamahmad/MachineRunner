version: 2.1
workflows:
  testing:
    jobs:
      - runner-test
jobs:
  runner-test:
    machine: true
    resource_class: ahteshamahmad/rhl_runner
    steps:
      - run: echo "Hi! This is my first runner"